

/**手机号码正则 */
export const phonePattern = /^1[3456789]\d{9}$/;

export const options = {//add global class in *
    addGlobalClass:true
}

import {$primaryColor} from '@src/styles/variables.json'
export const commonNavigationBarConfig = {
    navigationBarBackgroundColor: $primaryColor,
}   